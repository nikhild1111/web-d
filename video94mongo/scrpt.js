// mongo instance means we can work on multiple instance to store data different
//collection means collection of diffrent files or drive to store data sepretly in structured why
// in collection we insert the document
//each document have its own id
//persist means data is store perminently
//if we open multiple tab then data is store if pc shut down 
//but in mongo data is persist